import { StyledHeader, Start, Center, End } from "./styles";

const Header = () => {
  return (
    <StyledHeader>
      <Start />
      <Center />
      <End />
    </StyledHeader>
  );
};

export default Header;
