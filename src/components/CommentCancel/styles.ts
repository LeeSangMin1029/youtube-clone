import styled from 'styled-components';
import { BaseButton } from '@/styles/utils';

export const StyledExtendButton = styled(BaseButton)`
  height: 34px;
  padding: 0 14px;
  border-radius: 18px;
`;
